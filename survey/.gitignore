# secrets — the model API key lives here, NEVER commit it
.env

# scratch output. NOTE: demo/ is intentionally committed as a showcase
# (clone -> open dashboard works), but its intermediate crops/frames are not.
out/
frames/
crops/

# python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store
